# GitHub Pages собирает сайт через Jekyll. Папки из exclude не попадают в опубликованный сайт.
# Важно: если в корне появится файл .nojekyll, Jekyll отключается и exclude не действует —
# тогда в публикацию попадёт всё, включая _admin/. Подробности: _admin/README.md
exclude:
  - _admin
  - tools
